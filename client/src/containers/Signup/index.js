import React from "react";
import AuthenticationForm from "../../components/AuthenticationForm";

const Signup = () => {
  return <AuthenticationForm page={5} pageTitle="register" />;
};

export default Signup;
